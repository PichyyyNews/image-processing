# บทที่ 14: โครงงานพิเศษและการนำเสนอผลงาน (AI Computer Vision Mini-Project Showcase)
> **หลักสูตร:** การประมวลผลภาพดิจิทัล (Digital Image Processing)  
> **เครื่องมือ:** VS Code, Git, Python, OpenCV, PyTorch, YOLO, MediaPipe

---

## ภาพรวมของบทเรียน

สัปดาห์ที่ 14 เป็นสัปดาห์สำคัญที่นักศึกษาจะได้นำองค์ความรู้ทั้งหมดตลอด 13 สัปดาห์ ตั้งแต่การจัดการพิกเซลภาพ การกรอง Noise การตรวจจับขอบเขตและ Contour การใช้โมเดล Deep Learning (PyTorch CNN, ONNX, YOLO) ไปจนถึง Pose Landmark Estimation (MediaPipe) มาบูรณาการสร้างเป็นระบบซอฟต์แวร์ประยุกต์ใช้งานจริง (Real-world Application)

---

## บทที่ 1: มาตรฐานการจัดวางโครงสร้างไฟล์โปรเจกต์ (Project Directory Standard)

เพื่อให้โปรเจกต์มีระดับความเป็นมืออาชีพ สามารถนำไปต่อยอดและดีบักได้ง่าย ให้จัดวางโครงสร้างโฟลเดอร์ตามมาตรฐานดังนี้:

```
mini_project_name/
├── README.md                   ← เอกสารอธิบายวิธีติดตั้งและรันโปรเจกต์
├── requirements.txt            ← รายการแพ็คเกจที่ใช้
├── main.py                     ← ไฟล์หลักสำหรับสั่งรันระบบ
│
├── config/
│   └── settings.json           ← กำหนดค่า Threshold และพารามิเตอร์ระบบ
│
├── models/
│   └── best.onnx (หรือ best.pt) ← weights ของโมเดล AI
│
├── src/
│   ├── preprocessing.py        ← โมดูลทำความสะอาดภาพและปรับแสง
│   ├── detector.py             ← โมดูลเรียกใช้โมเดล AI ทำนาย
│   └── utils.py                ← ฟังก์ชันวาด GUI และตีกรอบ
│
└── assets/
    └── test_video.mp4          ← คลิปวิดีโอสำหรับทดสอบ
```

---

## บทที่ 2: ขั้นตอนการสอบนำเสนอและการสัมภาษณ์ดีบักสด

การประเมินผลในสัปดาห์ที่ 14 ประกอบด้วย 3 ส่วนสำคัญ:
1. **การบรรยายสไลด์ (10 นาที):** อธิบายปัญหา สถาปัตยกรรมระบบ และผลลัพธ์เชิงตัวเลข
2. **การสาธิตระบบสด (Live Demo 10 นาที):** เปิดรันโปรแกรมบน VS Code แสดงผลการทำงานผ่านกล้องสดหรือวิดีโอ
3. **การสัมภาษณ์สด (Code Walkthrough & Debugging Challenge 10 นาที):** ผู้สอนจะสุ่มถามตรรกะโค้ดในสคริปต์ และสั่งให้ปรับเปลี่ยนพารามิเตอร์สดๆ (เช่น เปลี่ยนสี Bounding Box, เพิ่ม Threshold การกรองพื้นที่) เพื่อประเมินว่านักศึกษาเป็นผู้เขียนโค้ดเอง 100%
