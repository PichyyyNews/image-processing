# โครงการสอน (Course Outline) - สัปดาห์ที่ 9
**หัวข้อ:** การก้าวเข้าสู่โมเดลประสาทเทียมและการจำแนกภาพแรกด้วย PyTorch
**เวลาเรียน:** 5 ชั่วโมง (บรรยาย 2 ชั่วโมง / ปฏิบัติการ 3 ชั่วโมง)
**เครื่องมือที่ใช้:** VS Code, Miniconda, PyTorch (`torch`, `torchvision`), OpenCV, Python

---

## 1. วัตถุประสงค์การเรียนรู้ (Learning Objectives)
เมื่อสิ้นสุดการเรียนในสัปดาห์นี้ นักศึกษาจะสามารถ:
1. **อธิบายแนวคิดพื้นฐาน** ของปัญญาประดิษฐ์แบบการเรียนรู้เชิงลึก (Deep Learning) และการทำหน้าที่ของโครงข่ายประสาทเทียมแบบคอนโวลูชัน (CNN) ในงานคอมพิวเตอร์วิทัศน์ได้
2. **เข้าใจโครงสร้างของ CNN** ประกอบไปด้วย Convolutional Layer, Activation Layer (ReLU), Pooling Layer และ Fully Connected (Linear) Layer
3. **ใช้งานเฟรมเวิร์ก PyTorch** ในการพัฒนาโครงสร้างโมเดลโครงข่ายประสาทเทียม คลาส Dataset และตัวช่วยจัดสรรโหลดข้อมูล DataLoader ได้อย่างถูกต้อง
4. **เขียนโปรแกรมฝึกฝนโมเดล (Training Loop)** โดยคำนวณหาค่าความสูญเสีย (Loss Function) และปรับปรุงค่าน้ำหนักผ่านการประดิษฐ์ Optimizer และระบบ Backpropagation ในแต่ละรอบการทำงาน (Epoch)
5. **วิเคราะห์ตัวชี้วัดประสิทธิภาพโมเดล** เช่น Accuracy, Loss รวมถึงรู้วิธีบันทึกโมเดล (`.pth`) และการนำไปโหลดใช้งานทำนายรูปภาพลายมือตัวเองผ่านไลบรารี OpenCV

---

## 2. แผนการสอนช่วงบรรยาย (Lecture Plan) - 2 ชั่วโมง

### 2.1 บทนำสู่ Deep Learning และ Neural Networks (30 นาที)
* **จากจุดสกัดจุดเด่นดั้งเดิมสู่การประเมินอัตโนมัติ**: ข้อแตกต่างของการกำหนดฟีเจอร์ด้วยตนเอง (Manual Feature Extraction เช่น SIFT/ORB) เปรียบเทียบกับการให้ตัวแบบเรียนรู้หาฟีเจอร์ด้วยตัวเอง (End-to-End Deep Learning)
* **โครงสร้างเซลล์ประสาทเทียม (Artificial Neuron / Perceptron)**:
  * ข้อมูลนำเข้า ($x$), ค่าน้ำหนัก ($w$), ค่าไบแอส ($b$)
  * การรวมแบบเชิงเส้น (Linear Combination: $z = \sum w_i x_i + b$)
  * ฟังก์ชันกระตุ้น (Activation Function) เช่น Sigmoid, Softmax เพื่อเพิ่มความเป็นไม่เชิงเส้น (Non-linearity)
* **Multi-Layer Perceptron (MLP)**: โครงข่ายประสาทแบบดั้งเดิมและการเชื่อมต่อเต็มรูปแบบ (Fully Connected)

### 2.2 โครงข่ายประสาทเทียมแบบคอนโวลูชัน (CNN - Convolutional Neural Networks) (45 นาที)
* **ทำไม MLP ทั่วไปจึงไม่เหมาะกับรูปภาพขนาดใหญ่?**: ปัญหาเรื่องการสูญเสียมิติตำแหน่ง (Spatial Information) และจำนวนพารามิเตอร์ที่มหาศาลเกินความจำเป็น
* **สถาปัตยกรรมหลักของ CNN**:
  * **Convolutional Layer**: การเลื่อนตัวกรอง (Kernels/Filters) สกัดคุณลักษณะเฉพาะ (เช่น เส้นขอบแนวตั้ง แนวนอน พื้นผิวลายจุด) ออกมาเป็น Feature Maps
  * **Activation Function (ReLU - Rectified Linear Unit)**: เปลี่ยนค่าที่เป็นลบทั้งหมดให้เป็นศูนย์ ($f(x) = \max(0, x)$) ช่วยให้โมเดลจำแพทเทิร์นที่ซับซ้อนและเรียนรู้ได้เร็วขึ้น
  * **Pooling Layer (Max Pooling)**: การย่อขนาดของรูปภาพโดยเลือกหยิบค่าพิกเซลสูงสุดจากพื้นที่ขนาดเล็ก ช่วยลดจำนวนพารามิเตอร์และเพิ่มคุณสมบัติความทนทานต่อตำแหน่งที่คลาดเคลื่อน (Translation Invariance)
  * **Fully Connected Layer (FC / Linear Layer)**: การนำเอาฟีเจอร์ที่สกัดได้จากเลเยอร์ก่อนหน้ามาแผ่ออกเป็นเวกเตอร์ 1 มิติ (Flatten) เพื่อทำการเชื่อมต่อคลาสในการทำนายผลลัพธ์ปลายทาง

### 2.3 กระบวนการเรียนรู้และตัวประเมินผลลัพธ์ (30 นาที)
* **การส่งข้อมูลไปข้างหน้า (Forward Propagation)** และการคำนวณความสูญเสีย (Loss Calculation)
* **ตัววัดความสูญเสีย (Loss Function)**: การวัดความต่างระหว่างค่าพยากรณ์และผลเฉลยจริง โดยใช้ Cross-Entropy Loss สำหรับจัดกลุ่มหลายคลาส (Multi-class Classification)
* **ตัวปรับค่าน้ำหนัก (Optimizer & Backpropagation)**: การใช้คณิตศาสตร์อนุพันธ์ระดับสูง (Chain Rule) ย้อนกลับไปปรับปรุงค่าน้ำหนักโดยใช้ Gradient Descent, อัลกอริทึม SGD (Stochastic Gradient Descent) และ Adam Optimizer
* **การประเมินผลความถูกต้อง**:
  * ความแม่นยำรวม (Accuracy) เทียบกับค่าสูญเสีย (Loss)
  * แนวคิดของตารางเปรียบเทียบคำทำนาย (Confusion Matrix)

### 2.4 สรุปบทบรรยายและถามตอบ (15 นาที)
* อภิปรายเปรียบเทียบวิธีการสกัดฟีเจอร์ระดับต่ำ (สัปดาห์ที่ 1-7) กับการเรียนรู้ระดับลึก (Deep Learning)

---

## 3. แผนการสอนช่วงปฏิบัติการ (Lab Plan) - 3 ชั่วโมง
**Lab 9: Build & Train CNN in VS Code**

### 3.1 การตรวจสอบและเตรียมความพร้อม Environment (15 นาที)
* เปิด VS Code Workspace และทดสอบการทำงานของ `check_env.py` ผ่าน Terminal
* ยืนยันการนำเข้า (Import) ไลบรารีหลักอย่าง `torch` (PyTorch) และ `torchvision` สำเร็จครบถ้วน

### 3.2 การโหลดข้อมูลและสร้างโครงสร้างคลังข้อมูล (DataLoader) (45 นาที)
* แนะนำให้รู้จักกับ **MNIST Dataset** คลังรูปภาพขาวดำของตัวเลขเขียนด้วยลายมือขนาด $28 \times 28$ พิกเซล จำนวน 10 คลาส (เลข 0-9)
* การใช้ `torchvision.datasets` ในการดาวน์โหลดและจัดสรรรูปภาพ
* ประยุกต์ใช้คลาส `transforms.Compose` เพื่อแปลงโครงสร้างไฟล์รูปภาพปกติให้อยู่ในรูปเวกเตอร์ Tensor ของ PyTorch พร้อม Normalization ข้อมูลพิกเซล
* ตั้งค่า `DataLoader` เพื่อทำหน้าที่ส่งรูปภาพออกไปให้แบบจำลองฝึกฝนทีละกลุ่มข้อมูล (Batch Size) และสลับตรรกะรูปภาพ (Shuffle) ในการลดปัญหาจำจำเจ

### 3.3 ออกแบบและสร้างโครงสร้าง Simple CNN (45 นาที)
* สร้างโครงสร้างไฟล์โมเดลโดยสืบทอดคุณสมบัติจาก `torch.nn.Module`
* กำหนด Layer ในเมธอด `__init__`:
  * เลเยอร์ Convolution 1-2 ระดับ (`nn.Conv2d`)
  * เลเยอร์ลดมิติ Max Pooling (`nn.MaxPool2d`)
  * เลเยอร์ทำนายผล Linear Layers (`nn.Linear`)
* กำหนดเส้นทางการประมวลผลในเมธอด `forward`

### 3.4 การเขียนคำสั่งลูปฝึกฝนและทอดสอบตัวแบบ (Train Loop & Inference) (75 นาที)
* เขียนฟังก์ชันการเทรน (Training Loop): วนลูปตามจำนวน Epoch ดึงข้อมูลแบบ Batch ออกจาก DataLoader เคลียร์ค่าเกรเดียนต์ (`optimizer.zero_grad()`) ส่งภาพเข้าโมเดลเพื่อพยากรณ์ หาค่าความเสียเปรียบผ่าน Loss ทำการคำนวณย้อนกลับ (`loss.backward()`) และอัปเดตน้ำหนัก (`optimizer.step()`)
* เขียนฟังก์ชันประเมิน (Validation/Evaluation Loop) บนข้อมูลชุดทดสอบโดยไม่คำนวณเกรเดียนต์ (`with torch.no_grad()`) เพื่อคำนวณหาค่า Accuracy
* สั่งบันทึกไฟล์น้ำหนักโมเดลเก็บไว้ในเครื่องในรูปแบบไฟล์สกุล `.pth`
* **งานท้ายบทเรียน (Inference Challenge)**: เขียนคำสั่งประมวลผลดึงภาพตัวเลขลายมือที่เขียนขึ้นเองนอกระบบ โหลดเข้ามาด้วย OpenCV ปรับขนาดขนาดภาพ ปรับสีสว่าง ขาวดำ และส่งผลลัพธ์ผ่านตัวแปลงพิกเซลให้โมเดลทำนายและแสดงผลลัพธ์สรุปออกทางหน้าจอ
