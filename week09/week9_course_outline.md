# โครงการสอน (Course Outline) - สัปดาห์ที่ 9
**หัวข้อ:** พื้นฐาน Machine Learning, การจัดการข้อมูล (Data Pipeline), และการก้าวเข้าสู่โมเดลประสาทเทียม (CNN) ด้วย PyTorch
**เวลาเรียน:** 5 ชั่วโมง (บรรยาย 2 ชั่วโมง / ปฏิบัติการ 3 ชั่วโมง)
**เครื่องมือที่ใช้:** VS Code, Miniconda, PyTorch (`torch`, `torchvision`), Scikit-learn, OpenCV, Python

---

## 1. วัตถุประสงค์การเรียนรู้ (Learning Objectives)
เมื่อสิ้นสุดการเรียนในสัปดาห์นี้ นักศึกษาจะสามารถ:
1. **อธิบายแนวคิดพื้นฐาน** ของการเรียนรู้ของเครื่อง (Machine Learning) ประเภท Supervised vs Unsupervised และขั้นตอน Data Pipeline (การรับข้อมูล, การเตรียมข้อมูล, และการแบ่งข้อมูล) ได้อย่างถูกต้อง
2. **เข้าใจและอธิบายหลักการทำงานของโมเดล Decision Tree** รวมถึงการนำคุณลักษณะ (Features) ที่ได้จากการประมวลผลภาพมาใช้ในการจำแนกรูปทรงได้
3. **คำนวณผลลัพธ์ของเซลล์ประสาทเทียมเดี่ยว (Perceptron/Neural Network)** ด้วยมือตามขั้นตอนคณิตศาสตร์ (การหาผลรวมเชิงเส้นและการผ่านฟังก์ชันกระตุ้น เช่น Step, Sigmoid, ReLU)
4. **อธิบายสถาปัตยกรรมหลักของ CNN** (Convolutional, ReLU, Max Pooling, Fully Connected Layers) และเหตุผลที่เหมาะกับงานด้านภาพดิจิทัล
5. **เขียนโปรแกรมประมวลผลข้อมูล (Preprocessing & Splitting)** และเขียนโค้ดฝึกสอนโมเดล Simple CNN บน PyTorch ตลอดจนการเซฟโมเดลและการทำ Inference ภาพตัวเลขเขียนมือของตนเองผ่าน OpenCV ได้

---

## 2. แผนการสอนช่วงบรรยาย (Lecture Plan) - 2 ชั่วโมง

### 2.1 บทนำสู่ Machine Learning และ Data Pipeline (25 นาที)
* **กระบวนการทำงานของ Machine Learning**: เปรียบเทียบกับการเขียนโปรแกรมแบบดั้งเดิม (Classical Programming vs Machine Learning)
* **ประเภทของ Machine Learning**: Supervised Learning (เรียนรู้แบบมีผู้สอน/มีเฉลย) และ Unsupervised Learning (เรียนรู้แบบไม่มีผู้สอน)
* **วงจรการจัดการข้อมูล (Data Pipeline)**:
  * **Get Data**: การรวบรวมข้อมูลภาพหรือคุณลักษณะจากภาพ
  * **Data Preprocessing**: การเตรียมข้อมูล (เช่น การปรับขนาดภาพ, การกลับสีภาพ, การทำ Normalization สเกลพิกเซล)
  * **Data Splitting**: ทำไมต้องแบ่งข้อมูลเป็น Train Set (สอนโมเดล), Validation Set (วัดผลเพื่อจูนโมเดล), และ Test Set (ทดสอบความถูกต้องขั้นสุดท้ายโดยที่โมเดลไม่เคยเห็น)?

### 2.2 โมเดลจำแนกประเภทแบบต้นไม้ตัดสินใจ (Decision Tree) (20 นาที)
* **แนวคิดของ Decision Tree**: การเรียนรู้โครงสร้างแบบโครงข่ายต้นไม้เพื่อหาเงื่อนไขในการจำแนกประเภท (Root Node -> Decision Node -> Leaf Node)
* **การจำแนกรูปทรงภาพด้วยลักษณะทางกายภาพ**: ตัวอย่างการตัดสินใจจำแนกรูปทรงเรขาคณิต (วงกลม, สามเหลี่ยม, สี่เหลี่ยม) จากคุณลักษณะภาพที่สกัดมา (เช่น การเช็กความโค้งมน, จำนวนมุมที่ตรวจพบ, สัดส่วนความกว้างยาว)

### 2.3 โครงข่ายประสาทเทียมเริ่มต้นและการคำนวณ Perceptron (35 นาที)
* **โครงสร้างเซลล์ประสาทเทียมเดี่ยว (Perceptron)**:
  * ข้อมูลนำเข้า ($x$), ค่าน้ำหนัก ($w$), ค่าไบแอส ($b$)
  * การรวมแบบเชิงเส้น (Linear Combination: $z = \sum w_i x_i + b$)
* **ฟังก์ชันกระตุ้น (Activation Function)**:
  * **Step Function**: แปลงเป็น 0 หรือ 1
  * **Sigmoid**: บีบค่าให้อยู่ในช่วง $[0, 1]$
  * **ReLU (Rectified Linear Unit)**: กรองค่าลบให้เป็น 0 ส่วนค่าบวกส่งต่อปกติ ($f(x) = \max(0, x)$)
* **การคำนวณด้วยมือ (Hand Calculation Walkthrough)**: แสดงการคำนวณทีละขั้นด้วยตัวเลขสมมติเพื่อให้เข้าใจกลไกการตัดสินใจภายในของโหนดประสาท

### 2.4 โครงข่ายประสาทเทียมแบบคอนโวลูชัน (CNN) และกระบวนการฝึกสอน (40 นาที)
* **ทำไมต้อง CNN?**: ข้อจำกัดของ MLP ในการใช้กับภาพความละเอียดสูง (ปัญหาจำนวนพารามิเตอร์และการสูญเสีย Spatial Information)
* **สถาปัตยกรรมหลักของ CNN**: Convolutional Layer (ใช้ Kernel สกัดฟีเจอร์), ReLU Activation, Max Pooling (ย่อยขนาดภาพ), Fully Connected Layer (จำแนกคลาสสุดท้าย)
* **Training Dynamics (กระบวนการวนซ้ำ)**: Forward Pass -> Loss Calculation (Cross-Entropy) -> Backpropagation (กฎลูกโซ่) -> Weight Update (Optimizer: SGD/Adam)

---

## 3. แผนการสอนช่วงปฏิบัติการ (Lab Plan) - 3 ชั่วโมง
**Lab 9: ML Foundations, Data Pipeline & CNN in PyTorch**

### 3.1 การตรวจสอบและเตรียมความพร้อม Environment (15 นาที)
* ตรวจสอบการติดตั้งแพ็กเกจ `scikit-learn` และไลบรารีพื้นฐานอื่นๆ ใน Conda `dip_env`

### 3.2 ปฏิบัติการ Data Pipeline & Decision Tree (45 นาที)
* ฝึกเขียนสคริปต์โหลดคุณลักษณะภาพเรขาคณิตจำลอง (กว้าง, ยาว, จำนวนมุม, ความโค้งมน)
* ทำ Preprocessing (การสเกลข้อมูลค่าพิกเซลและคุณลักษณะ)
* แบ่งข้อมูลออกเป็น Train และ Test Set ด้วยคำสั่ง `train_test_split`
* ฝึกฝนและทดสอบโมเดล Decision Tree ด้วยคลาส `DecisionTreeClassifier` ของ Scikit-learn

### 3.3 ปฏิบัติการ Perceptron Hand Calculation & Code (30 นาที)
* ทำใบงานฝึกคำนวณค่าค่าน้ำหนักและไบแอสของ Perceptron ด้วยตนเอง
* เขียนฟังก์ชันภาษา Python จำลองการทำงานของโหนด Perceptron และฟังก์ชันกระตุ้นต่างๆ เพื่อยืนยันผลการคำนวณด้วยมือ

### 3.4 โครงสร้างโมเดล Simple CNN และการเตรียมชุดข้อมูล MNIST (45 นาที)
* สร้างโครงสร้างไฟล์โมเดลสืบทอดจาก `torch.nn.Module`
* นิยามคลาสเตรียมข้อมูล (Dataset) และส่งต่อเข้าคลาสจัดแบ่งกลุ่ม (DataLoader) ของ MNIST

### 3.5 การเขียน Train Loop และท้าทายการทำ Inference ด้วยภาพจริง (45 นาที)
* เขียนโครงสร้างลูปสำหรับการวนลูปการฝึกสอน (Forward, Loss, Backward, Step)
* สั่งบันทึกไฟล์น้ำหนักโมเดลเก็บไว้ในรูปแบบไฟล์สกุล `.pth`
* **งานท้ายบทเรียน (Inference Challenge)**: วาดภาพตัวเลขเขียนมือภายนอก โหลดด้วย OpenCV ปรับขนาด 28x28 และปรับแต่งสี (ดำหลัง ขาวเลข) เพื่อส่งเข้าไปทำนายและทดสอบประสิทธิภาพโมเดลสดๆ
