# โครงร่างสไลด์ (Slide Outline) - สัปดาห์ที่ 9: การก้าวเข้าสู่โมเดลประสาทเทียมและการจำแนกภาพแรกด้วย PyTorch

---

## Slide 1: Title
*   **หัวข้อ:** การก้าวเข้าสู่โมเดลประสาทเทียมและการจำแนกภาพแรกด้วย PyTorch
*   **สัปดาห์ที่:** 9
*   **รายวิชา:** การประมวลผลภาพดิจิทัล (Digital Image Processing)

---

## Slide 2: วัตถุประสงค์การเรียนรู้ (Learning Objectives)
*   เข้าใจแนวคิดการสืบสานข้อมูลและการเรียนรู้แบบปลายทางต่อปลายทาง (End-to-End Deep Learning)
*   เข้าใจสถาปัตยกรรมหลักของ CNN (Conv2d, ReLU, MaxPool2d, Linear)
*   เขียนโปรแกรมสร้าง CNN และจัดการรูปภาพผ่านคลาส Dataset/DataLoader ของ PyTorch ได้
*   ออกแบบคำสั่งวนลูปประมวลผลเพื่อฝึกสอนโมเดล (Training Loop) และเข้าใจกลไกการคำนวณ Loss/Backpropagation
*   สามารถนำข้อมูลผลเฉลยของน้ำหนักโมเดลสเกลตัวเลขเขียนลายมือ (MNIST) มาเขียนแอปพลิเคชันทำนายภาพจริงด้วย OpenCV ได้

---

## Slide 3: Hand-Crafted Features vs. Deep Learning
*   **แบบดั้งเดิม (Classical Machine Learning):**
    *   เราต้องสกัดฟีเจอร์เอง (เช่น ใช้ SIFT/ORB, หา Edge, หา Contours)
    *   จากนั้นจึงส่งเวกเตอร์คุณลักษณะไปให้โมเดลจำแนกผล (เช่น SVM, Decision Trees)
    *   หากภาพมืด แสงเปลี่ยน หรือวัตถุเบี้ยว อัลกอริทึมสกัดฟีเจอร์อาจทำงานล้มเหลว
*   **แบบเรียนรู้เชิงลึก (Deep Learning / CNN):**
    *   รวบขั้นตอนการดึงฟีเจอร์และการจำแนกประเภท (End-to-End) เข้าไว้ด้วยกัน
    *   ตัวแปรน้ำหนักในฟิลเตอร์จะถูกปรับแต่งโดยอัตโนมัติผ่านการคำนวณย้อนกลับ (Backpropagation)
    *   เรียนรู้รูปแบบฟีเจอร์จากระดับต่ำสุด (ขอบ/มุม) ไปสู่ระดับสูง (รูปร่าง/ชิ้นส่วนวัตถุ)

---

## Slide 4: เซลล์ประสาทเทียมพื้นฐาน (Artificial Neuron)
*   **Perceptron (เซลล์ประสาทเทียมตัวเดี่ยว):**
    *   รับตัวแปรนำเข้า $x_1, x_2, \dots, x_n$
    *   คูณค่าน้ำหนัก (Weights: $w_1, w_2, \dots, w_n$) บวกค่าเอนเอียง (Bias: $b$)
    *   $z = w_1 x_1 + w_2 x_2 + \dots + w_n x_n + b$
    *   ส่งผ่านฟังก์ชันกระตุ้น (Activation Function) เช่น Sigmoid หรือ Softmax เพื่อสร้างความน่าจะเป็น
*   **Multi-Layer Perceptron (MLP):**
    *   การนำเซลล์ประสาทหลายตัวมาต่อกันเป็นเลเยอร์ (Input -> Hidden -> Output Layers)
    *   มีข้อจำกัดเมื่อเจอข้อมูลรูปภาพ: ทำให้รูป 2 มิติเสียข้อมูลเชิงมิติตำแหน่ง (Spatial) เมื่อแผ่เป็น 1 มิติ และพารามิเตอร์มีจำนวนมากเกินไปจนเกิดปัญหา Overfitting

---

## Slide 5: โครงสร้างหลักของ CNN (CNN Core Layers)
*   **CNN (Convolutional Neural Network):** โครงข่ายประสาทที่มีโครงสร้างออกแบบมาสำหรับข้อมูลที่มีมิติเชิงพื้นที่ เช่น รูปภาพ
*   **4 เลเยอร์สำคัญที่ประกอบร่าง:**
    1.  **Convolutional Layer ($nn.Conv2d$):** ใช้ Kernel/Filter สไลด์หาคุณลักษณะเด่น
    2.  **Activation Layer ($nn.ReLU$):** ตัดค่าลบทิ้ง เพิ่มความยืดหยุ่นทางคณิตศาสตร์แบบไม่เป็นเส้นตรง (Non-linear)
    3.  **Pooling Layer ($nn.MaxPool2d$):** ย่อยขนาดภาพเพื่อช่วยลดมิติและพารามิเตอร์
    4.  **Fully Connected Layer ($nn.Linear$):** สรุปฟีเจอร์ทั้งหมดทำนายคลาสปลายทาง

---

## Slide 6: เลเยอร์ Convolution & Feature Maps
*   **การดำเนินการคอนโวลูชัน (Convolution Operation):**
    *   ตัวกรองขนาดเล็ก (เช่น $3 \times 3$ หรือ $5 \times 5$ พิกเซล) จะทำการคูณจุดต่อจุดกับภาพต้นฉบับแล้วรวมค่าเป็นพิกเซลเดี่ยวในหน้าต่างใหม่
    *   เลื่อนตัวกรองไปเรื่อยๆ จนทั่วทั้งภาพ (ตามค่า Stride และ Padding ที่กำหนด)
    *   ผลลัพธ์ที่ได้เรียกว่า **Feature Map**
*   **ความมหัศจรรย์:** ฟิลเตอร์แต่ละตัวจะตรวจจับรูปแบบต่างกัน เช่น ฟิลเตอร์หนึ่งหาขอบซ้าย อีกตัวหาขอบขวา โดยค่าน้ำหนักในฟิลเตอร์จะเรียนรู้ด้วยตัวเองระหว่างเทรนโมเดล

---

## Slide 7: ReLU Activation & Max Pooling
*   **ReLU (Rectified Linear Unit):**
    *   สูตร: $f(x) = \max(0, x)$
    *   หากผลลัพธ์จาก Convolution มีค่าเป็นลบ จะปัดเป็น $0$ ทันที หากเป็นบวกให้ผ่านตามปกติ
    *   ป้องกันปัญหาความลาดชันหาย (Vanishing Gradient) และประมวลผลได้เร็วมาก
*   **Max Pooling ($2 \times 2$ Stride 2):**
    *   แบ่งภาพออกเป็นบล็อกย่อยขนาด $2 \times 2$ พิกเซล และเลือกดึงเฉพาะค่าสูงสุดของบล็อกมาสร้างภาพใหม่
    *   ช่วยย่อยขนาดกว้างและสูงลงครึ่งหนึ่ง แต่ยังคงฟีเจอร์เด่นที่สำคัญไว้

---

## Slide 8: Fully Connected (Linear) Layer
*   **การแผ่ข้อมูล (Flatten):**
    *   หลังจากสกัดข้อมูลผ่านบล็อก Convolution และ Max Pooling แล้ว ภาพจะถูกเปลี่ยนโครงสร้างจาก Matrix 2 มิติ (หรือ 3 มิติเชิงลึก) ให้แผ่ออกเป็นเส้นตรงยาว 1 มิติ
*   **การทำนายผลลัพธ์ (Classification):**
    *   ส่งต่อเวกเตอร์ที่แผ่ยาวให้แก่ Linear Layers ซึ่งเปรียบเสมือนสมองส่วนสรุปผลวิเคราะห์
    *   สเกลผลลัพธ์ออกตามจำนวนคลาสเป้าหมาย (เช่น ในชุด MNIST มีเลข 0-9 แปลว่ามี Output ขนาด 10 ตัวเลข)
    *   ใช้ Softmax เพื่อแปลงคะแนนของแต่ละคลาสให้อยู่ในรูปของความน่าจะเป็น (Probability) รวมกันได้ $100\%$ ($1.0$)

---

## Slide 9: Loss Function, Optimizers & Backpropagation
*   **ความสูญเสีย (Loss Function):**
    *   คำนวณเปรียบเทียบผลลัพธ์พยากรณ์กับเฉลยจริง เช่น ภาพเป็นเลข "3" แต่โมเดลตอบความน่าจะเป็นว่าเป็นเลข "8"
    *   เราใช้ **Cross-Entropy Loss** ในการหาค่าเบี่ยงเบนความถูกต้องของคำตอบ
*   **การปรับปรุงแบบจำลอง (Backpropagation & Optimizers):**
    *   เมื่อได้ค่า Loss จะทำการดึงข้อมูลอนุพันธ์ย้อนกลับ (Backpropagate gradients) จากหลังสุดมาหน้าสุด เพื่อหาว่าค่าน้ำหนักจุดใดควรปรับลดหรือเพิ่ม
    *   **Optimizer** (เช่น SGD, Adam) จะทำหน้าที่อัปเดตน้ำหนักตามทิศทางคณิตศาสตร์ลาดชัน (Gradient Descent)
    *   ทำซ้ำทีละ Batch วนลูปจบครบรูปทั้งหมดเรียกว่า 1 **Epoch**

---

## Slide 10: ตัววัดประสิทธิภาพของตัวแบบ (Evaluation Metrics)
*   **Accuracy (ความถูกต้องรวม):**
    *   $\text{Accuracy} = \frac{\text{จำนวนภาพที่ทำนายถูกทั้งหมด}}{\text{จำนวนภาพทั้งหมด}}$
*   **Precision, Recall, F1-Score:**
    *   มีความสำคัญสูงหากชุดข้อมูลมีความไม่สมดุล (Imbalance Class)
*   **Confusion Matrix (ตารางความสับสน):**
    *   ตารางเปรียบเทียบจำนวนการตอบถูกและตอบผิดในแต่ละคลาส ช่วยให้ครูและนักศึกษารู้ว่าโมเดลมักสับสนเลขคู่ใดมากที่สุด (เช่น แยกแยะเลข 3 กับเลข 8 ไม่ออก)

---

## Slide 11: ภาคปฏิบัติการ - LAB 9
*   **โจทย์:** Build & Train CNN in VS Code
*   **แผนการปฏิบัติ:**
    1. ตรวจสอบเวอร์ชันผ่าน `check_env.py`
    2. เขียนโปรแกรมโหลด MNIST และจัดการโหลดข้อมูลเป็น Batch ด้วย `DataLoader`
    3. สร้างคลาสโมเดล CNN `SimpleCNN` สืบทอดจาก `nn.Module`
    4. พัฒนาคำสั่งลูปเทรนและทดสอบวัดผลค่า Loss / Accuracy
    5. ส่งออกและบันทึกโมเดลไว้ในไฟล์น้ำหนัก `.pth`
    6. โหลดโมเดลไปทดสอบทำนายรูปภาพลายมือของนักศึกษาเองที่เตรียมจากภายนอกโดยใช้ OpenCV ปรับพิกเซลสี
